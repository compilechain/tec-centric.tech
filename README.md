# tec-centric.tech
single home, shared issues, easy refactors; Cloudflare Pages supports setting different “root directories” per project (marketing → /marketing, blog → /blog, app → /app). Netlify mirror (marketing) can also target /marketing
